Data Stucts
dir_list
   d_name[malloc] 
   depth
   next
free_list of dir_list struct
   
main
   argv = dir

   add_queue( dir, depth=-1) (top of tree has -1 for depth; unique)
   create mutex
   create 128 theads
   pthread_create(&threadcX, NULL, walk, X); pass thread number;
   pthread_exit(NULL); 

Thread ( int index )
   while ( 1 ) {
     if ( dir_list = get_work(index) == NULL ) {  /* no work to do */
        is workers?  if queue is empty and nobody is working; end
     opendir( dir_list.path+dir_list
   


    
